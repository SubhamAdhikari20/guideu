import 'package:flutter/material.dart';

/// GuideU brand palette — Himalaya blues with the logo's red accent.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF1E63D8); // Himalaya blue
  static const Color secondary = Color(0xFF12B5C9); // teal
  static const Color accent = Color(0xFFE2231A); // GuideU red
  static const Color background = Color(0xFFF7F9FC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF0B1F3A);
  static const Color textSecondary = Color(0xFF5B6B82);
  static const Color error = Color(0xFFE2231A);
}
